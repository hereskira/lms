<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Grades</title>
    <link href="styles.css" rel="stylesheet">
</head>
<body>
    <div class="grades-container">
        <h2>Welcome back <%= user.fname %> <%= user.mname %> <%= user.lname %>!</h2>
        <% if (grades) { %>
        <table>
            <thead>
                <tr>
                    <th>Subject</th>
                    <th>Grade</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Math</td>
                    <td><%= grades.math %></td>
                </tr>
                <tr>
                    <td>Science</td>
                    <td><%= grades.science %></td>
                </tr>
                <tr>
                    <td>English</td>
                    <td><%= grades.english %></td>
                </tr>
                <tr>
                    <td>PE</td>
                    <td><%= grades.pe %></td>
                </tr>
            </tbody>
        </table>
        <% } else { %>
        <p>No grades available.</p>
        <% } %>

        <% if (user.role === 'teacher') { %>
        <h3>Update Grades</h3>
        <form action="/update-grades" method="POST">
            <div>
                <label for="math">Math</label>
                <input type="text" id="math" name="math" value="<%= grades.math %>">
            </div>
            <div>
                <label for="science">Science</label>
                <input type="text" id="science" name="science" value="<%= grades.science %>">
            </div>
            <div>
                <label for="english">English</label>
                <input type="text" id="english" name="english" value="<%= grades.english %>">
            </div>
            <div>
                <label for="pe">PE</label>
                <input type="text" id="pe" name="pe" value="<%= grades.pe %>">
            </div>
            <button type="submit">Update Grades</button>
        </form>
        <% } %>
    </div>
</body>
</html>
